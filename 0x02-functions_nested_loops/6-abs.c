#include "main.h"
/**
 * _abs - Absolute value computation
 * @i : is an integer
 * integer - the data type computed and returned
 * Return: Absolute value
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
